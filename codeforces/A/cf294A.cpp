#include <iostream>

#define ll long long

int main() {
    ll n, a[n], m, xy[m][2], out[100];

    std::cin >> n;
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
    }

    std::cin >> m;
    for (int i = 0; i < m; i++) {
        std::cin >> xy[i][0] >> xy[i][1];
    }

           
}
